// Server connection URL
// When running locally, Vite proxies /socket.io to the server.
// If accessing via localtunnel, we just use the current origin.
export const SERVER_URL = window.location.origin;
